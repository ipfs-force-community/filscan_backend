package chain

import (
	"testing"
)

func TestNewReward(t *testing.T) {
	
	//r := NewReward("f0567567", 2399890, AttoFil(decimal.NewFromFloat(240)))
	//
	//releases, err := r.Releases()
	//require.NoError(t, err)
	//
	//for _, vv := range releases.list {
	//	t.Logf("日期: %s 高度: %d 奖励: %s 天: %d", vv.Epoch().Date(), vv.Epoch(), vv.Reward().Decimal(), vv.Day())
	//}
	//
	//t.Log(releases.TotalReleasesAfterEpoch(2912530))
}
